# ChampForge 컨텍스트 노트

## 2026-05-11 기획 시작

사용자는 포켓몬 챔피언스 배틀 도움 사이트를 원한다. 대상은 상급자이며, 핵심 목적은 추천 파티 만들기다.

선택된 MVP 기능은 다음과 같다.

- 데미지 계산기.
- 팀 빌더.
- 카운터 추천.

디자인 방향은 게임 공략 위키처럼 정보 밀도 높은 PC 중심 UI다. 로그인과 계정 기반 파티 저장이 필요하다.

## 현재 데이터 기준에 대한 결정

과거 본가식 `H252` 표기를 기본으로 쓰지 않는다. 사용자가 지적한 대로 포켓몬 챔피언스에서는 `H32` 같은 현재식 스탯 포인트 표기를 기준으로 삼아야 한다.

따라서 UI, 저장 데이터, 팀 빌더의 1급 표기는 SP 기준으로 둔다. 내부 계산에서 필요한 경우에만 공식 계산용 값으로 변환한다.

## 외부 정보 확인 결과

공식 포켓몬 사이트는 포켓몬 챔피언스가 기존 포켓몬 배틀과 같은 타입, 특성, 기술 기반 전략을 사용하며, 단순화된 훈련 시스템과 Pokémon HOME 연동을 제공한다고 설명한다. 또한 2026년 4월과 5월부터 공식 경쟁 환경이 포켓몬 챔피언스로 전환된다고 안내한다.

공개 커뮤니티와 계산기 자료에서는 챔피언스의 스탯 시스템이 기존 252 EV 표기가 아니라 0부터 32까지의 스탯 포인트 중심으로 다뤄지고 있으며, 총량 제한과 Lv.50 기준 계산을 중요하게 본다. 이 부분은 공식 문서로 완전히 닫힌 사양이 아니라, 구현 전 실제 인게임 검증과 신뢰 가능한 계산기 비교가 필요하다.

## 이름 결정

서비스 이름은 **ChampForge**로 정했다.

의미는 챔피언스 환경에서 파티를 단련하고 빚어낸다는 뜻이다. 너무 포켓몬 공식 브랜드처럼 보이지 않으면서도 배틀 분석 도구의 성격을 전달한다.

## 목표 전략 기반 추천 결정

추천 파티 생성은 “핵심 포켓몬 하나를 고르면 나머지 다섯 마리를 자동 추천”보다 “목표 전략을 먼저 고르는 방식”이 더 적합하다.

상급자는 보통 단일 포켓몬보다 다음과 같은 게임 플랜을 먼저 잡는다.

- 빠른 공격형.
- 밸런스.
- 트릭룸.
- 날씨.
- 필드.
- 사이클.
- 벽 전개.
- 메가 에이스 중심.

따라서 팀 빌더는 목표 전략을 선택하고, 그 전략의 필수 역할 슬롯을 채우는 방식으로 설계한다.

## 다음 구현 전 확인할 것

현재 프로젝트는 인테리어 업체 랜딩 페이지로 구현되어 있다. ChampForge를 이어서 만들려면 기존 랜딩 페이지를 완전히 교체할지, 브랜치를 새로 따거나 별도 디렉터리로 분리할지 먼저 결정해야 한다.

계산기는 챔피언스 전용을 목표로 하지만, 공개 정보와 인게임 검증이 필요한 영역이 있다. 특히 SP가 내부 계산에서 어떤 값으로 환산되는지, IV가 완전히 고정인지, 성향 보정과 레벨 50 계산이 어떤 순서로 적용되는지 테스트 케이스가 필요하다.

## 2026-05-11 구현 시작

사용자가 구현 시작을 승인했다.

현재 저장소는 인테리어 랜딩 페이지가 루트 화면을 차지하고 있으므로, 별도 앱 분기 없이 루트 화면을 ChampForge 프로토타입으로 교체한다. 이유는 사용자가 “지금 사이트 결과물”을 확인하고 이어서 구현하자고 했기 때문에 같은 프로젝트에서 바로 눈에 보이는 결과물을 만드는 것이 적합하기 때문이다.

첫 구현은 백엔드와 실제 계정 저장까지 가지 않고, 프론트엔드 상호작용 프로토타입을 우선 만든다. 포함 범위는 목표 전략 선택, 6마리 팀 슬롯, SP 표기 입력, 샘플 기반 데미지 계산, 카운터 추천, 로그인/저장 패널이다.

## 2026-05-11 프로토타입 구현 결과

루트 화면을 ChampForge 프로토타입으로 교체했다. 구현된 화면은 PC 중심 2단 구조다.

- 왼쪽 고정 사이드바. 검색, 기능 네비게이션, 저장 파티 목록.
- 상단 전략 선택. 메가 에이스 밸런스, 트릭룸 컨트롤, 빠른 공격형, 날씨 사이클.
- 팀 빌더. 6마리 슬롯, 역할 라벨, 타입 배지, 선택 슬롯 편집.
- 세트 편집기. 포켓몬 교체, 특성, 도구, 기술, SP 분배, Lv.50 추정 실능 표시.
- 라이브 분석. 물리/특수 압박, 속도 제어, Fake Out 수, 타입 리스크.
- 데미지 계산기. 공격자, 기술, 방어자 선택 후 샘플 공식 기반 데미지 범위 표시.
- 카운터 추천. 주요 위협 포켓몬과 위험도 표시.
- 데이터 허브. SP 우선 모델, 룰 버전 고정, 검증 대기 항목 요약.

로그인과 저장은 실제 백엔드 연동이 아니라 프론트엔드 목업이다. 저장 버튼은 현재 전략명을 저장 목록에 추가한다. 다음 단계에서 Auth.js, DB, 실제 Team 모델을 연결해야 한다.

검증은 `npm run lint`와 `npm run build`로 통과했다.

개발환경에서 `.env.local`이 없어도 `/api/auth/providers`를 확인할 수 있도록 dev 전용 fallback secret을 넣었다. 프로덕션에서는 `AUTH_SECRET`을 반드시 설정해야 한다.

## 2026-05-12 파티 저장 구현 계획

OAuth 다음 단계로 파티 저장 UX를 실제 동작하게 만든다. 아직 DB와 OAuth 키가 준비되지 않았으므로 서버 저장이 아니라 브라우저 localStorage 저장으로 구현한다.

저장 키는 `champforge:teams:{email|guest}` 형식이다. 로그인 상태면 이메일 기준, 비로그인 상태면 guest 기준으로 저장한다. 저장 데이터는 전략 이름, 6마리 포켓몬 영문 키, 생성 시각, 표시 라벨만 가진다.

이 방식은 실제 계정 간 동기화가 되지 않는 임시 구현이다. 이후 Prisma와 DB를 붙일 때 같은 `SavedTeam` 형태를 서버 모델로 옮기면 된다.

## 2026-05-12 파티 저장 구현 결과

브라우저 localStorage 기반 저장과 불러오기를 구현했다. 사이드바 저장 버튼은 비로그인 상태에서도 guest 키로 저장하고, 로그인 상태 상단 패널의 파티 저장 버튼은 현재 세션 이메일 키로 저장한다.

저장된 파티를 클릭하면 전략과 6마리 포켓몬 슬롯을 복원한다. 저장 메시지와 빈 상태도 표시한다.

검증은 `npm run lint`와 `npm run build`로 통과했다. 실제 클릭 저장은 브라우저 localStorage 기능이므로 수동 UI 확인이 추가로 필요하다.

## 2026-05-12 Regulation M-A 실제 데이터 수집

사용자가 현재 포켓몬 챔피언스에 있는 포켓몬만 실제 데이터로 가져오라고 요청했다.

`scripts/fetch-champions-regulation-ma.py`를 추가했다. 이 스크립트는 Bulbapedia Regulation Set M-A 원문에서 `CPCard` 목록을 추출하고, PokeAPI로 한글명, 타입, 종족값, 특성, official artwork URL을 보강한다.

생성된 데이터 파일은 `src/data/champions-regulation-ma.ts`다.

현재 생성 결과는 다음과 같다.

- eligible 포켓몬 213개.
- 허용 메가진화 59개.

앱의 팀 빌더 선택 목록과 데미지 계산기 공격자/방어자 선택은 이제 하드코딩 8마리가 아니라 이 213개 데이터를 사용한다. 저장 데이터는 이름 중복을 피하기 위해 포켓몬 이름 대신 데이터 `id` 배열을 저장하도록 바꿨다.

주의할 점은 다음과 같다.

- official artwork는 PokeAPI sprites를 사용한다.
- 챔피언스 신규 메가진화의 실제 메가 종족값은 아직 앱 계산에 반영하지 않았다.
- 기술, 도구, 특성의 챔피언스 전용 합법성은 아직 별도 데이터로 검증하지 않았다.

검증은 `npm run lint`, `npm run build`, 로컬 `/` 응답 200으로 통과했다.

## 2026-05-12 특성과 기술명 한글화

사용자가 스킬과 특성을 한글화해 달라고 요청했다.

수집 스크립트에 PokeAPI ability 리소스의 한국어 이름을 가져오는 단계를 추가했다. 현재 Regulation M-A eligible 포켓몬들이 가진 고유 특성 180개의 한글명 맵을 `regulationMAAbilityNameKo`로 생성한다.

앱 화면에서는 세트 편집기의 특성 표시가 영어 slug 대신 한국어 이름을 사용한다. 예를 들면 `intimidate` 대신 `위협`을 표시한다.

기술은 아직 전체 기술 합법성 데이터가 없으므로, 현재 프로토타입 계산기와 기본 세트에서 사용하는 기술만 우선 한국어 표시명을 붙였다. 다음 단계에서 챔피언스의 전체 기술 목록과 합법성 데이터를 별도 수집해야 한다.

## 2026-05-12 OP.GG 표기 보정

사용자가 한글 번역이 이상하고 OP.GG를 참고하라고 지적했다.

OP.GG 포켓몬 챔피언스 기술/특성 페이지를 기준으로 화면에 노출되는 샘플 기술명을 보정했다. 기존의 임의 샘플명 `Aerial Rend`를 제거했고, `Earth Power`를 `그라운드포스`로 표시하던 오류를 `대지의힘`으로 고쳤다. 물, 불꽃, 풀 기본 기술도 계산 샘플로 쓰기 쉬운 `파도타기`, `화염방사`, `에너지볼`로 바꿨다.

특성은 PokeAPI 한국어 이름을 기본으로 유지하되, OP.GG식 표기와 화면 품질을 맞추기 위해 주요 특성명 오버라이드 레이어를 추가했다. 전체 기술 합법성 데이터는 아직 별도 수집이 필요하다.

## 2026-05-12 데미지 계산기 확장 계획

사용자가 계산기도 구현해보자고 요청했다.

현재 계산기는 공격자, 기술, 방어자만 선택하는 목업이므로 실제 도구처럼 쓰기 어렵다. 이번 단계에서는 서버 계산 엔진이나 전체 기술 합법성 DB까지 만들지 않고, 화면 안에서 바로 조정 가능한 계산기로 확장한다.

성공 기준은 다음과 같다.

- Lv.50 기본값을 유지하되 레벨을 조정할 수 있다.
- 공격 SP, 방어 HP SP, 방어 내구 SP를 직접 입력할 수 있다.
- 공격/방어 성격 보정, 랭크 변화, 화상, 더블 배틀 분산, 벽 보정을 입력할 수 있다.
- STAB, 타입 상성, 85-100 난수 16단계를 반영한다.
- 결과에는 실능, 배율 근거, 최소/최대 데미지, HP 비율, KO 판정을 같이 표시한다.

챔피언스의 세부 데미지 공식이 공개 자료만으로 완전히 닫힌 사양은 아니므로, 계산기는 “Lv.50, IV 31, SP×8을 EV 상당값으로 환산”하는 현재 가정을 UI에 명시한다.

## 2026-05-12 데미지 계산기 확장 결과

루트 화면의 데미지 계산기를 확장했다.

계산 엔진은 공격/방어 실능 계산, 공격/방어 랭크, STAB, 타입 상성, 화상, 더블 배틀 분산, 벽, 급소, 85-100 난수 16단계를 반영한다. 결과 화면에는 최소/최대 데미지, 방어자 HP 기준 비율, 1타 난수 수, KO 판정, 공격/방어 실능, STAB/상성/총 보정을 표시한다.

UI는 공격자, 기술, 방어자 선택 아래에 레벨, 공격 SP, 방어 HP SP, 방어 내구 SP, 성격 보정, 랭크 보정, 주요 체크 보정을 배치했다. 모바일에서는 입력 그리드가 1열로 접히도록 CSS를 보강했다.

검증은 `npm run lint`, `npm run build`, 로컬 `/` 응답 200으로 통과했다. Browser 스킬의 Node 기반 브라우저 제어 도구가 이 세션에 노출되지 않아 인앱 브라우저 스크린샷 검증은 하지 못했다.

## 2026-05-12 smartnuo 계산기 기준 재조정 계획

사용자가 계산기에는 레벨 입력이 필요 없고, smartnuo.com에 있는 기능은 모두 있어야 한다고 지적했다.

smartnuo 첫 화면에서 확인한 계산기 입력군은 공격 측 포켓몬, 기술, 타입, 위력, 공격 수치, 개체값, 실수값, 랭크업, 레벨, 결정력, 성격 보정, 배수, 상태이상, 특성, 날씨, 도구, 필드, 급소, 충전, 도우미, 배터리, 파워스폿, 플라워기프트, 강철정신, 페어리오라, 다크오라, 오라브레이크, 재앙 계열이다. 방어 측은 포켓몬, 타입 2개, 체력 수치, 방어 수치, 개체값, 실수값, 랭크업, 내구력, 성격 보정, 특성, 도구, 리플렉터, 빛의장막, 오로라베일, 프렌드가드다.

ChampForge는 챔피언스 전용이므로 레벨 입력은 제거하고 Lv.50 고정으로 둔다. smartnuo의 레벨 항목은 내부 상수로만 유지한다.

## 2026-05-12 smartnuo 계산기 기준 재조정 결과

계산기 화면에서 레벨 입력을 제거하고 Lv.50을 고정값으로 표시했다. 공격 측에는 타입, 위력, 배수, 상태이상, 공격 SP/개체값/실수값, 성격, 랭크, 특성, 날씨, 도구, 필드, 급소, 충전, 도우미, 배터리, 파워스폿, 플라워기프트, 강철정신, 오라, 재앙 계열, 더블 분산 입력을 추가했다.

방어 측에는 HP SP/개체값/실수값, 방어 SP/개체값/실수값, 성격, 랭크, 특성, 도구, 리플렉터, 빛의장막, 오로라베일, 프렌드가드 입력을 추가했다. 결과에는 데미지 범위, 1타 난수, 결정력, 내구력, STAB, 상성, 총 보정을 표시한다.

검증은 `npm run lint`, `npm run build`로 통과했다.

## 2026-05-12 계산기 페이지 분리 계획

사용자가 계산기 페이지를 아예 별도로 구성하자고 요청했고, 현재 구현이 완벽한지 물었다.

현재 계산기는 완벽하지 않다. smartnuo의 주요 입력군은 갖췄지만, 챔피언스 전용 정확 공식 검증, 전체 기술/특성/도구 합법성 DB, 특성별 예외 처리, 복수 타격/고정 데미지/방어 무시/테라스탈 같은 특수 룰 처리는 아직 없다. 따라서 계산기를 홈의 하위 패널이 아니라 독립 페이지로 분리하고, 화면 안에 “현재 모델의 가정과 남은 리스크”를 명확히 표시한다.

## 2026-05-12 계산기 페이지 분리 결과

`/calculator` 전용 페이지를 추가했다. 홈 사이드바의 데미지 계산기 링크도 `/calculator`로 연결했다.

계산기 페이지는 공격 측, 방어 측, 하단 고정 결과 패널로 구성했다. 좌측 사이드바에는 워크벤치 복귀, 계산기, 결과, 검증 메모 링크와 현재 계산기가 완벽하지 않은 이유를 명시했다.

검증은 `npm run lint`, `npm run build`, 로컬 `/calculator` 응답 200으로 통과했다.

## 2026-05-12 계산기 자동 동기화 계획

사용자가 타입 같은 값은 포켓몬 선택 시 자동으로 가져와야 한다고 지적했다.

방어 타입은 이미 포켓몬 선택에서 자동 표시하고 있지만, 공격 기술 타입과 위력은 수동 입력처럼 노출되어 혼란스럽다. 전용 계산기와 홈 패널 모두에서 기술 선택 시 타입과 위력을 자동 갱신하고, 포켓몬 선택 시 해당 포켓몬의 기본 SP를 계산 입력에 반영한다. 타입과 위력 입력은 “수동 보정” 성격을 분명히 표시한다.

## 2026-05-12 계산기 자동 동기화 결과

`/calculator`와 홈 계산기 패널 모두에서 기술 선택 시 타입과 위력이 자동 갱신되도록 수정했다. 공격자 선택 시 공격 기술 분류에 맞는 A/C 기본 SP를 다시 잡고, 방어자 선택 시 HP SP와 B/D 기본 SP를 다시 잡는다.

실수값 직접 입력은 포켓몬을 바꾸면 이전 포켓몬 기준 값이 남지 않도록 초기화한다. 타입과 위력 필드는 “수동 보정” 라벨로 바꿔 기본값이 자동으로 들어온 뒤 사용자가 예외 상황에서만 덮어쓰는 입력임을 드러냈다.

검증은 `npm run lint`, `npm run build`로 통과했다.

## 2026-05-12 홈 계산기 패널 제거

사용자가 계산기 페이지를 따로 빼라고 다시 지적했다.

기존 구현은 `/calculator`를 만들었지만 홈 하단에 계산기 패널을 그대로 남겨 두어 흐름이 어색했다. 홈에서는 계산기 입력 UI를 제거하고, 사이드바의 데미지 계산기 링크가 `/calculator` 전용 페이지로 이동하는 구조만 남겼다.

검증은 `npm run lint`, `npm run build`, 로컬 `/calculator` 응답 200으로 진행한다.

## 2026-05-11 한글명과 이미지 적용 계획

사용자가 포켓몬 명을 한글로 가져오고 캐릭터 이미지를 표시할 수 있는지 물었다. 프로토타입 단계에서는 PokeAPI sprites 저장소의 official artwork URL을 직접 참조한다.

한글명은 우선 앱의 샘플 데이터에 직접 포함한다. 추후 실제 데이터 파이프라인을 만들 때 PokeAPI species의 다국어 이름 또는 별도 한국어 도감 데이터를 수집해 동기화한다.

Mega Dragonite처럼 PokeAPI에 별도 폼 이미지가 없는 샘플은 망나뇽 기본 official artwork를 대체 이미지로 사용한다. 실제 챔피언스 전용 폼 아트가 필요하면 공식 사용 권한과 자산 출처 확인이 필요하다.

## 2026-05-11 한글명과 이미지 적용 결과

샘플 포켓몬 데이터에 `displayName`, `dex`, `image` 필드를 추가했다. 화면에서는 한글명을 우선 표시하고, 내부 선택값과 계산 키는 기존 영문 `name`을 유지한다.

팀 슬롯과 세트 편집기에 PokeAPI sprites official artwork 이미지를 표시한다. Next.js `remotePatterns`에 `raw.githubusercontent.com/PokeAPI/sprites/.../official-artwork/**`를 등록했고, 일반 `<img>` 대신 `next/image`를 사용한다.

검증은 `npm run lint`와 `npm run build`로 통과했다. 샘플 이미지 URL은 HEAD 요청 기준 200 응답을 확인했다.

## 2026-05-11 Regulation M-A 샘플 정합성 수정

사용자가 샘플에 현재 포켓몬 챔피언스 Regulation M-A에 없는 포켓몬이 섞였다고 지적했다. 확인 결과 기존 샘플에는 `날개치는머리`, `뽀록나`, `우라오스`, `고릴타`, `토네로스`처럼 M-A eligible 목록에 없는 포켓몬이 포함되어 있었다.

샘플 풀을 현재 M-A 목록에 있는 포켓몬 기준으로 교체했다. 현재 샘플은 메가 망나뇽, 가디안, 어흥염, 키키링, 킬라플로르, 패리퍼, 코터스, 대도각참이다.

위협 목록도 현재 M-A에 맞춰 메가 프테라, 메가 이상해꽃, 메가 팬텀, 키키링 중심으로 바꿨다.

## 2026-05-11 OAuth 로그인 구현 계획

사용자가 Google과 Kakao 로그인을 원했다. Auth.js 공식 문서 기준으로 `next-auth@beta`를 사용한다.

구현 범위는 다음과 같다.

- `src/auth.ts`에서 Google, Kakao Provider 설정.
- `src/app/api/auth/[...nextauth]/route.ts` 라우트 핸들러 추가.
- 클라이언트 `SessionProvider` 추가.
- 상단 버튼을 실제 `signIn("google")`, `signIn("kakao")` 호출로 교체.
- 로그인 후 사용자명, 이메일, 로그아웃 버튼 표시.

실제 OAuth 동작에는 `.env.local`의 `AUTH_SECRET`, `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`, `AUTH_KAKAO_ID`, `AUTH_KAKAO_SECRET` 값이 필요하다. 개발 콜백 URL은 Google과 Kakao 콘솔 모두 `http://localhost:3000/api/auth/callback/{provider}` 형태로 등록해야 한다.

## 2026-05-11 OAuth 로그인 구현 결과

`next-auth@beta`를 설치하고 Auth.js v5 방식으로 Google과 Kakao Provider를 연결했다. `/api/auth/[...nextauth]` 라우트가 생성되었고, 상단 로그인 영역은 세션 상태에 따라 Google/Kakao 로그인 버튼 또는 사용자 정보와 로그아웃 버튼을 표시한다.

`AUTH_SETUP.md`와 `.env.example`을 추가했다. 실제 로그인을 테스트하려면 사용자가 Google Cloud Console과 Kakao Developers에서 OAuth 앱을 만들고 콜백 URL을 등록해야 한다.

현재 저장 기능은 아직 DB 저장이 아니라 UI 버튼만 존재한다. 다음 단계에서 유저별 파티 저장 모델과 서버 액션을 연결해야 한다.

검증은 `npm run lint`와 `npm run build`로 통과했다.
